# Example Skill: Shell Access
This skill allows the agent to inspect the local filesystem and use shell commands when that is the most direct path to completion.

## Key Capabilities
- List files (`ls`)
- Read files (`read_file`)
- Search content (`grep_search`)
- Execute custom scripts (`execute_command`)

## Usage
Used for tasks that require direct filesystem interaction or running external tools.
